import Cpc.Proofs.TypePreservation.Full

/-!
Top-level bridge to the full type-preservation development.

Modules outside `Cpc.Proofs.TypePreservation.*` should prefer importing this
file rather than reaching into the `TypePreservation/` internals directly when
they intentionally need the full proof stack.
-/
