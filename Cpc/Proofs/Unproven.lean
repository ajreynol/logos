import Cpc.Proofs.Translation

/-!
Legacy compatibility shim.

The lightweight translation surface now lives in `Cpc.Proofs.Translation`, and
the full proofs live in `Cpc.Proofs.Translation.Full`.
-/
